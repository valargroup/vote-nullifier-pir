[package]
name = "pir-types"
version = "0.3.0"
edition = "2021"
description = "Shared wire types and tier-layout constants for Ironwood nullifier non-membership proofs."
license = "MIT OR Apache-2.0"
repository = "https://github.com/valargroup/vote-nullifier-pir"
readme = "README.md"

[features]
default = ["upstream"]
upstream = ["voting-crypto-deps?/upstream", "imt-tree?/upstream"]
zakura = ["voting-crypto-deps?/zakura", "imt-tree?/zakura"]
reader = ["dep:voting-crypto-deps", "dep:ff", "dep:anyhow", "dep:imt-tree"]

[dependencies]
serde = { version = "1", features = ["derive"] }
voting-crypto-deps = { workspace = true, optional = true }
ff = { version = "0.13", optional = true }
anyhow = { version = "1.0", optional = true }
imt-tree = { workspace = true, optional = true }

[dev-dependencies]
serde_json = "1.0"
