[workspace]
resolver = "2"
members = [
    "imt-tree",
    "nf-server",
    "pir/client",
    "pir/export",
    "pir/server",
    "pir/test",
    "pir/types",
    "nf-ingest",
]

[workspace.package]
rust-version = "1.91"

[workspace.dependencies]
imt-tree = { path = "imt-tree", version = "0.5.2", default-features = false }
voting-crypto-deps = { version = "=0.2.2", default-features = false }

[profile.release]
lto = "fat"
codegen-units = 1
